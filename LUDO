#include <stdio.h>

int main(void) {
    int T, x;
    scanf("%d", &T);
    
    for (int i = 0; i < T; i++) {
        scanf("%d", &x);
        if (x == 6) {
            printf("YES\n");
        } else {
            printf("NO\n");
        }
    }
    
    return 0;
}
