#include <stdio.h>

int main(void) {
    int t, x;
    scanf("%d", &t); // Read the number of test cases

    while (t--) {
        scanf("%d", &x); // Read the frequency for the current test case
        
        if (x >= 67 && x <= 45000) {
            printf("YES\n");
        } else {
            printf("NO\n");
        }
    }
    
    return 0;
}
