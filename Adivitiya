#include <stdio.h>

int main(void) {
    int N;
    scanf("%d", &N);

    if (N >= 16 && N <= 18) {
        printf("ADVITIYA\n");
    } else {
        printf("WAITING FOR ADVITIYA\n");
    }

    return 0;
}
