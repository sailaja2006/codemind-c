#include <stdio.h>

int main(void) {
    int t, x;
    scanf("%d", &t); // Read the number of test cases

    while (t--) {
        scanf("%d", &x); // Read the value of x for each test case

        if (x >= 30) {
            printf("YES\n");
        } else {
            printf("NO\n");
        }
    }

    return 0;
}
